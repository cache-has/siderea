# Rust
target/

# Node
node_modules/

# Build outputs
dist/
build/
pkg/

# SvelteKit
.svelte-kit/

# WASM build output (committed for Cloudflare Pages deployment)
# Regenerate locally with: npm run build:wasm

# OS
.DS_Store

# Raw data (downloaded, not committed — regenerate with scripts/process-hyg.py)
data/raw/

# Source texture downloads (large files — regenerate with npm run fetch:textures)
data/textures-source/

# Processed texture LOD tiers (committed for Cloudflare Pages deployment)
# Regenerate locally with: npm run build:textures

# Processed data artifacts (committed for Cloudflare Pages deployment)
# Regenerate locally with: npm run build:data

# Environment
.env
.env.*
!.env.example
